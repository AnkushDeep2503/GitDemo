lists = [23, 45, 65, 87, 93] #lists elements can be enclosed within a []
print (lists)
print(lists[4]) # for accessing the list element index number for ex a[0], a[1] and so on.
lists[3] = 70 # for assigning a new value in the imdex number of list
print (lists)

b = [6.7, 8765, "Surbhi", True, 6.7] # We can create a list of items with different/any data types
print(b)

print(b.count(6.7)) # displanys the count of 6.7